<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.huntercat</groupId>
        <artifactId>lieshoucloud-services-open</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>lieshoucloud-user</artifactId>
    <name>LieShouCloud · User Service</name>
    <description>用户服务</description>

    <dependencies>
        <dependency>
            <groupId>cn.huntercat</groupId>
            <artifactId>framework-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.huntercat</groupId>
            <artifactId>framework-service</artifactId>
        </dependency>
        <!-- Web（MVC） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- JPA + PostgreSQL -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!--
          Flyway（Phase 6 数据库迁移 · schema 唯一权威，见 ADR-0021）
          - flyway-database-postgresql 是 Flyway 11 的 PG 方言模块（必须）
          - 版本由 spring-boot-dependencies 3.5.7 BOM 管理（flyway.version=11.7.2）
        -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Nacos 注册 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>

        <!-- Nacos Config（Phase 9 · P1-5 配置中心 · ADR-0029；默认关闭，见 application.yml） -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>

        <!-- actuator -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- SpringDoc OpenAPI (WebMVC) - Phase 5 跨端契约 -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <!-- 底座共享：统一异常体 + 全局异常处理器（L2-1 · Bottom-Up） -->
        <dependency>
            <groupId>cn.huntercat</groupId>
            <artifactId>framework-common</artifactId>
        </dependency>

        <!-- Spring Security crypto (BCrypt only; NOT full Security framework) -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>

        <!-- 阿里云短信 SDK（生产验证码通道 · ADR-0023 Phase 2 · 版本由 parent dependencyManagement 统一） -->
        <dependency>
            <groupId>com.aliyun</groupId>
            <artifactId>dysmsapi20170525</artifactId>
        </dependency>

        <!-- SMTP 邮件（生产验证码通道 · ADR-0023 Phase 2 · 飞书邮箱） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
          Testcontainers PostgreSQL（Phase 6 起 @DataJpaTest 用真 PG，不再用 H2）
          Flyway 迁移脚本为 PG 方言，H2 无法验证；版本由 spring-boot-dependencies 3.5.7 BOM 管理。
          见 .ai/TESTING.md §2 / §9。
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
